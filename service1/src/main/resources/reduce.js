var reduce=function(key,values){
    //var val={ages:values.ages,name:values.name};
/*    values.forEach(function(p){
    val.ages=p.ages;
    val.name=p.name;
    val.elements+=p.elements;
    val.count+=2;
    
    });*/
    
  return "ali";
}
